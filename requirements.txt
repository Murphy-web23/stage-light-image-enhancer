# Image processing (shared by both frontends).
# The contrib build is required for cv2.dnn_superres (2x upscaling); without it
# the app still runs and falls back to Lanczos interpolation.
opencv-contrib-python-headless>=4.10,<5
numpy>=1.26.0
Pillow>=10.0.0

# FastAPI web app (api.py)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# Streamlit prototype (app.py)
streamlit>=1.36.0
